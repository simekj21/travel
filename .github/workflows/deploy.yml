name: Deploy on FTP hosting

on:
  push:
    branches: [main]

jobs:
  ftp-deploy:
    name: FTP deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inject cache-busting build version
        run: sed -i "s/__BUILD__/${{ github.sha }}/g" index.html

      - name: FTP deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ vars.FTP_SERVER_DIR || './' }}
          exclude: |
            **/.git/**
            **/.github/**
            uploads/originals/**
            uploads/thumbs/**
            actions/**
            data/photos.json
            data/tags.json
            data/events.json
            data/settings.json
